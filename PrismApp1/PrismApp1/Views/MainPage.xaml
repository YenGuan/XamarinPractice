<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="PrismApp1.Views.MainPage"
             Title="MainPage">
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <Label Text="{Binding Title}" />
        <Button Text="showNew1Page" Command="{binding showNew1PageCommand}"></Button>
        <Button Text="DeepLinking" Command="{binding deepLinkingCommand}"></Button>
        <Button Text="ShowDialog" Command="{binding dialogCommand}"></Button>
        <Button Text="User" Command="{binding userCommand}"></Button>
        <Label Text="{Binding UserName}" />
    </StackLayout>
</ContentPage>